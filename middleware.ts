// Basic auth for Vercel Edge Middleware (no Next.js imports).
// Set BASIC_AUTH_USER and BASIC_AUTH_PASS in Vercel environment variables.
// Keep a tiny JS middleware alternative in middleware.js for Vercel Edge runtime
// (TypeScript checker in this project can be noisy). See middleware.js
